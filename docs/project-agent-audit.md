# Project agent audit, 2026-09-06

Scope: mila-nextjs only; [model guide](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra), [OpenLoop PR 176](https://github.com/stozo04/OpenLoop/pull/176). Read root AGENTS/PROMPT/FEATURE-PROMPT/BUG-PROMPT, README and supporting architecture/monthly documents, all local skill instructions, feature recipes, helper source, adapter metadata, and each differing harness version. Excluded dependencies, build output, archived snapshots and other worktrees from the source inventory. The existing mobile-nav PR is unrelated; used a separate branch/worktree from origin/main and left it untouched.

Shared project and architecture guidance preserve unique content from AGENTS and PROMPT, product specifications, feature review, bug reproduction, required build/lint/type gates, privacy, and release/write boundaries. Corrected stale Node/Next/runtime guidance against package.json and the current route. Marked older architecture documents as historical; no schema examples or product code were applied. Identical AGENTS/CLAUDE pointers and Cursor's always-applied rule use the shared documents. Next's supported agentRules: false prevents dev startup from replacing pointers; the installed-framework documentation requirement remains shared and mandatory.

Reconciliation: prepare-milas-month and its OpenAI adapter metadata already matched and remain intact. verify-mila differed in Bash versus PowerShell recipes and own-tree references. Retained both shells, Windows Git Bash protection, process ownership cleanup, all feature expectations, and helper safety checks. Replaced mandatory Claude-only browser tool names with the available browser adapter's documented discovery/DOM facilities. Reconciled in the Claude tree before source-preserving repair; it was a working source, not an assumed authority. Three complete packages now have 19 files each (2 skills). Only own-tree references differ. Existing native OpenAI metadata remains in its supported agents/openai.yaml format.

The 32 tracked generated response artifacts were moved with git mv to docs/verification-evidence under their original harness names. Every file retains its bytes; no evidence was discarded or recopied into a missing harness. Future artifacts and credential sessions are gitignored in all three trees. Old evidence is historical, not proof for this commit.

Reused the reference checker and 16 regression cases with remote-default resolution. npm run lint and thus existing PR CI check the mirrors. The full live sweep still applies to runtime-impacting auth/routing/rendering changes; agent-file generation alone is scoped to startup/pointer proof plus the required static/build gates, as requested by the prompting audit. No failed feature expectation was changed to pass.

Validation: 16 checker cases pass, including both path slash forms, identical foreign references, source-preserving repair, and missing/drifted files. Sync passes, 19 files x 3. Root pointers and links checked, all 32 evidence moves verified against Git blobs, each helper passes node --check and four unsafe-request refusal cases, and all harness session/evidence ignore rules pass. npm run lint, next typegen, tsc --noEmit, and npm run build pass; .next/BUILD_ID exists. A real Next dev startup detected Codex, reached Ready, and preserved identical pointers without regenerating a block. Only this run's server was stopped and its port verified closed. Final diff inspected and git diff --check passes.

Skipped: full live route/browser sweep, OAuth/admin sessions, monthly database checks, uploads, creation, publishing, TTS/chat calls, and deployment. No request-handling, UI, auth, or database behavior changed. Build/startup used non-secret placeholder public configuration and made no live application requests. Historical known feature findings remain documented; this audit does not claim them fixed. No merge, release or deployment performed.
